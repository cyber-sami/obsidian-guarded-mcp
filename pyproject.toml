[project]
name = "obsidian-guarded-mcp"
version = "1.0.0"
description = "Local MCP server that gives Claude safe, multi-vault read and write access to Obsidian notes."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.0,<4",
    "python-frontmatter>=1.1,<2",
]

[tool.uv]
package = false
